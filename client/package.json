{
  "name": "client",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "next start",
    "build": "next build",
    "deploy": "now && now alias && now rm messenger --safe --yes",
    "dev": "next -p 3500"
  },
  "dependencies": {
    "babel-plugin-transform-inline-environment-variables": "^0.4.3",
    "next": "^6.0.2",
    "react": "^16.3.2",
    "react-dom": "^16.3.2",
    "socket.io-client": "^2.1.0"
  },
  "devDependencies": {
    "babel-plugin-inline-dotenv": "^1.1.2",
    "prettier": "^1.12.1"
  }
}
